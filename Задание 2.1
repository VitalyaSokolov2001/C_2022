/*******************************************************************************

Получить от пользователя два числа А, В (В>A). Пройти циклом от А до В, вывести значение каждого
числа умноженного на 3 в файл «output»

****************************************************************************/
#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ofstream file ("Numbers");
        int A,B;
        cout << "Введите A" << '\n';
        cin >> A;
        cout << "A = " << A << '\n';
        cout << "Введите B>A" << '\n';
        cin >> B;
        cout << "B = " << B << '\n';
        for(int i=A;i<=B;i++){
        int C=i*3;
        file << C << "\n";
        }
        file.close();
        cout << "Файл создан";
return 0;
}
