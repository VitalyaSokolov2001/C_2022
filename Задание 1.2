/******************************************************************************

Используя другой тип цикла, перебрать числа от 1 до 1000 с шагом 4. Найти сумму
всех числе, которые делятся на 7 без остатка.

*******************************************************************************/
#include <stdio.h>
int main()
{
    int b;
    int c=0;
    while (b<=1000)
    {   if(b % 7 == 0)
        { c = c + b; 
        }
        b = b + 4;  
    }
    printf("The sum of the numbers divisible by seven: %d", c);
    return 0;
}
